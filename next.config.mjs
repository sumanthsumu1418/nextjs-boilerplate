/** @type {import('next').NextConfig} */

// next.config.js
const nextConfig = {
    images: {
      domains: ['via.placeholder.com'],
      domains: ['cdn-icons-png.flaticon.com'], // Add other domains as needed
    },
  };

export default nextConfig;
